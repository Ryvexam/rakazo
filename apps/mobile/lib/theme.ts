/** Product palette for Expo. Prefer these over hardcoded Ryvoko hexes.
 * System chrome (nav bars, fills, labels) stays in `./native` via PlatformColor. */
export { botColors, tokens } from "@ryvoko/ui-tokens";
